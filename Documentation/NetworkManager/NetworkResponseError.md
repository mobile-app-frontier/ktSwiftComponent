# NetworkResponseError

normal error code
1xx :​ info
2xx :​ success
3xx :​ redirection
4xx:​ client error
5xx:​ server error

``` swift
public struct NetworkResponseError : Error
```

## Inheritance

`Error`
